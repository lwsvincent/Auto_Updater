<Window x:Class="MainApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MainApp"
        mc:Ignorable="d"
        Title="Main Application - v1.0.2" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Background="White">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Main Application"
                       FontSize="32"
                       FontWeight="Bold"
                       Foreground="#2196F3"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <TextBlock Text="Version 1.0.2"
                       FontSize="20"
                       Foreground="Green"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30"/>

            <TextBlock Text="This is the main application launched by the updater."
                       FontSize="14"
                       Foreground="#666666"
                       HorizontalAlignment="Center"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       MaxWidth="600"
                       Margin="0,0,0,20"/>

            <Border Background="#F5F5F5"
                    Padding="20"
                    CornerRadius="8"
                    Margin="0,20,0,0">
                <StackPanel>
                    <TextBlock Text="✓ Auto-updater working correctly"
                               FontSize="14"
                               Margin="0,0,0,10"/>
                    <TextBlock Text="✓ Main application launched successfully"
                               FontSize="14"
                               Margin="0,0,0,10"/>
                    <TextBlock Text="✓ Configuration loaded from config.ini"
                               FontSize="14"/>
                </StackPanel>
            </Border>

            <Button x:Name="CloseButton"
                    Content="Close Application"
                    Width="200"
                    Height="40"
                    FontSize="14"
                    Margin="0,30,0,0"
                    Click="CloseButton_Click"
                    Background="#2196F3"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
